// Array countries
const arrayEuroCountries = ['France', 'Norway', 'Sweden', 'Germany', 'Spain', 'Italy', 'Andorra', 'Netherlands', 'United Kingdom', 'Ireland', 'Poland', 'Switzerland', 'Finland', 'Czech Rep.', 'Greece', 'Austria', 'Slovakia', 'Slovenia', 'Montenegro', 'Serbia', 'Croatia', 'Bosnia', 'Bulgaria', 'Macedonia', 'Hungary', 'Romania', 'Malta', 'Portugal', 'Iceland', 'Belarus', 'Lithuania', 'Latvia', 'Estonian', 'Denmark', 'Monaco', 'Belgium', 'Luxembourg', 'Albania', 'San Marino', 'Vatican', 'Liechtenstein', 'Ukraine', 'Kosovo', 'Cyprus'];
const arrayEuroCountriesSort = arrayEuroCountries.sort();


/*
const spanMinutes = document.querySelector('span#minutes');
const spanSeconds = document.querySelector('span#seconds');
// Milliseconds for the calculations

const milliSecondsOfaSecond = 1000;
const milliSecondsOfaMinute = milliSecondsOfaSecond * 60;
const milliSecondsOfaHour = milliSecondsOfaMinute * 60;
const milliSecondsOfaDay = milliSecondsOfaHour * 24

//===
// FUNCTIONS
//===

// updates the countdown and the sample

function updateCountdown() {
    // Calcs
    const duration = setInterval(30000);
    const remainingMinutes = Math.floor((duration % milliSecondsOfaHour) / milliSecondsOfaMinute);
    const remainingSeconds = Math.floor((duration % milliSecondsOfaMinute) / milliSecondsOfaSecond);
   

    // Render
    spanMinutes.textContent = remainingMinutes;
    spanSeconds.textContent = remainingSeconds;
}


updateCountdown();
// Refresh every second
setInterval(updateCountdown, milliSecondsOfaSecond);
*/